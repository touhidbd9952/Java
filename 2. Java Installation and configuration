1. Java install in computer
2. configure (path setting)

Solution
--------------
1. Download jdk (Java development kit for windows) and install
2. configure: after install jdk,
              Step1. Go to c:\\Program Files\Java\jdk-18.0.2\bin
              copy above path
              ---------------
              
              Step2. In computer, This pc --> properties --> Advanced system settings --> Environment Variable
              
              System variables
              -----------------------------------------------------------------
              Path (select it)
              
              -----------------------------------------------------------------
              
              click on "Edit" button --> "New" button and paste path here (copied) --> click on "Move Up" button several time to raise the path to the top
              
              --> Ok
              --> Ok
              --> Ok

3. Check Java install properly or not:
            1. go to command prompt (cmd)
            2. check java version: java -version (enter)
            3. java feature: java (enter)
            
